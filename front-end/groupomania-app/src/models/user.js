export default class User {
    constructor(avatar, privilege, username, email, password) {
        this.avatar = avatar;
        this.privilege = privilege;
        this.username = username;
        this.email = email;
        this.password = password;
    }
}